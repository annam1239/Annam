#include<stdio.h>
int main()
{
    int num,reverse=0,reminder,orginal;
    printf("entrer the number int");
    scanf("%d",&num);
    while(num!=0){
        reminder=num%10;
        reverse=num*10+reminder;
        num/=10;
            if("num=orginal"){
                printf("enter the number is palidrome");
               } else {
                    printf("enter the number is not palidrome");
                }
            }
        }
